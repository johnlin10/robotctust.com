export default function CourseWorkspaceAsideSlot() {
  // 返回 null，讓 /dashboard/courses/courses/[courseId] 這個特殊單頁
  // 不需要顯示「課程管理」側欄，由 CourseWorkspace 內部全螢幕處理，
  // 或改未來在此檔案回傳 Course 專用的快速大綱/設定表單 Aside
  return null
}
